This week I have implemented a dummy feedforward network which uses a sigmoid activation function (numerically stable version). I also implemented numerically stable softmax function. I read chapters 1 and 2 from Michael Nielsen's book (provided in the sources). I learned about sigmoid neurons, gradient descent and studied backpropagation mathematically. I have also done testing with pytest and made use of Poetry to handle dependencies (all tests have passed). Also, I have implemented load functionality. The total amount of time I have spent this week sums up to 13 hours. So far nothing is unclear to me, but the challenging part is understanding how to do data loading and which libraries to use. Next week, I will try to complete the network.py file with SGD, backpropagation functions as well as other useful functions involved in the cycle.